# https://app.circleci.com/pipelines/github/abphilip-resources/DO-CI-3/1/workflows/50451746-d041-42e1-bda0-082f293d74b0

version: 2.1                                      # CircleCI version

jobs:
  Build:                                          # Job name
    working_directory: ~/circleci-python          # Working directory
    docker:
      - image: "circleci/python:3.6.4"            # Docker image to be used
    steps:                                        # Steps to be executed
      - checkout                                  # Checkout the repository
      - run: python3 main.py                      # Execute the main.py file
  Test:                                           # Job name
    working_directory: ~/circleci-python          # Working directory
    docker:
      - image: "circleci/python:3.6.4"            # Docker image to be used
    steps:                                        # Steps to be executed
      - checkout                                  # Checkout the repository
      - run: python3 test.py                      # Execute the test.py file

workflows:                                        # Workflows to be executed
  Learn 0:                                        # Workflow name
    jobs:
      - Build:                                    # Job name
          requires: []                            # Dependencies
      - Test:                                     # Job name
          requires: [Build]                       # Dependencies          